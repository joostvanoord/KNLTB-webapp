# KNLTB Checker

Lokaal:
```sh
npm install
npm run login
npm start
```

Render:
1. Deploy deze map.
2. Run lokaal `npm run login`.
3. Kopieer de lange output naar Render env var `KNLTB_STATE`.
4. Open je Render-url.
